import {fetchJson} from './providers';
import {parseProvider,xSearchSchema} from './schemas';
export type OriginEvidence={url:string|null;createdAt:number|null;ageMinutes:number|null;author:string|null;confidence:'medium'|'unknown';method:string};
export async function findXOrigin(token:string,rawPhrase:string):Promise<OriginEvidence>{const phrase=rawPhrase.replace(/["“”]/g,'').replace(/https?:\/\/\S+/g,' ').replace(/\s+/g,' ').trim().slice(0,100);if(phrase.length<2)throw Error('Enter at least 2 characters to research an origin.');const params=new URLSearchParams({query:`"${phrase}" -is:retweet`,max_results:'100','tweet.fields':'created_at,author_id',expansions:'author_id','user.fields':'username'});const data=parseProvider(xSearchSchema,await fetchJson('https://api.x.com/2/tweets/search/recent?'+params,token),'X');const users=new Map((data.includes?.users??[]).flatMap(u=>u.id&&u.username?[[u.id,u.username] as const]:[]));const posts=(data.data??[]).filter(p=>p.id&&p.created_at).sort((a,b)=>Date.parse(a.created_at??'')-Date.parse(b.created_at??''));const first=posts[0];if(!first?.id||!first.created_at)return{url:null,createdAt:null,ageMinutes:null,author:null,confidence:'unknown',method:'No matching evidence found in the X recent-search window.'};const createdAt=Date.parse(first.created_at);const author=(first.author_id?users.get(first.author_id):undefined)??null;return{url:author?`https://x.com/${author}/status/${first.id}`:`https://x.com/i/status/${first.id}`,createdAt,ageMinutes:Number.isFinite(createdAt)?Math.max(0,Math.round((Date.now()-createdAt)/60000)):null,author,confidence:'medium',method:'Earliest matching post returned by X recent search; this is evidence, not a guarantee of the absolute first internet post.'};}
