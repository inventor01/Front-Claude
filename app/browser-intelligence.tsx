'use client';

import { useEffect, useMemo, useRef, useState } from 'react';
import { Activity, ExternalLink, ShieldCheck, X } from 'lucide-react';

const BRIDGE = 'http://127.0.0.1:43981';
const LOCAL_TIMEOUT_MS = 30_000;
const DEEP_SCAN_TIMEOUT_MS = 10 * 60_000;

type BridgeConfig = {enabled:boolean;intervalMinutes:number;scanXExplore:boolean;scanXHome:boolean;scanTikTokTrends:boolean;scanTikTokExplore:boolean;scrollPasses:number;maxFeedItems:number;inferredTopicSearches:number;maxTrendQueries:number;resultsPerQuery:number;xAccounts:string[];keywords:string[]};
type Evidence = {id:string;platform:'X'|'TikTok';author:string;url:string;content:string;published:number|null;views:number|null;likes:number|null;provenance:string};
type RelatedContext = {key:string;title:string;relation:string;score:number;evidenceCount:number;authorCount:number;platforms:string[];evidenceIds:string[]};
type InferredTopic = {topic:string;key:string;evidenceCount:number;authorCount:number;platforms:string[];oldestPublished:number|null;newestPublished:number|null;engagementEvidence:number;score:number;corroborated:boolean;evidenceIds:string[];relatedContexts?:RelatedContext[]};
type ScanResult = {evidence:Evidence[];errors:string[];inferredTopics?:InferredTopic[];at:number;config:BridgeConfig};
type Narrative = {id:string;title:string;stage:string;authors:number;firstSeen:number;lastSeen:number;platforms:string[]};
type StoredResult = {accepted?:number;rejected?:number;freshNarratives?:number;matchedNarratives?:number;freshCoins?:number;inferredNarratives?:number;relationshipsSaved?:number;error?:string};
type BridgeHealth = {config:BridgeConfig;version:number;running:boolean;pendingCount?:number;nextScheduledRun?:number|null;scanConnection?:string;lastError?:string|null};
const DEFAULT_CONFIG:BridgeConfig={enabled:true,intervalMinutes:15,scanXExplore:true,scanXHome:true,scanTikTokTrends:true,scanTikTokExplore:true,scrollPasses:4,maxFeedItems:40,inferredTopicSearches:4,maxTrendQueries:3,resultsPerQuery:6,xAccounts:[],keywords:[]};

async function local<T>(path:string,init?:RequestInit,timeoutMs=LOCAL_TIMEOUT_MS):Promise<T>{
 const controller=new AbortController();const timer=window.setTimeout(()=>controller.abort(),timeoutMs);
 try{
  const response=await fetch(BRIDGE+path,{...init,signal:controller.signal});
  const data=await response.json() as T&{error?:string};
  if(!response.ok)throw new Error(data.error||'Local browser bridge request failed.');
  return data;
 }catch(e){
  if(e instanceof DOMException&&e.name==='AbortError'){
   if(path==='/scan')throw new Error('The deep scan exceeded the web panel timeout. The local bridge may still be finishing the scan; check Browser Sources/health before starting another one.');
   throw new Error('The local browser bridge request timed out.');
  }
  throw e;
 }finally{window.clearTimeout(timer);}
}
async function saveEvidence(evidence:Evidence[],inferredTopics:InferredTopic[]=[]){const response=await fetch('/api/browser-evidence',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({evidence,inferredTopics})});const data=await response.json() as StoredResult;if(!response.ok)throw new Error(data.error||'Could not save browser evidence.');return data;}
async function fetchNarratives():Promise<Narrative[]>{const response=await fetch('/api/desk?action=narratives',{cache:'no-store'});const data=await response.json() as {cards?:Narrative[];error?:string};if(!response.ok)throw new Error(data.error||'Could not refresh narratives.');return data.cards||[];}
function lines(value:string){return[...new Set(value.split(/[\n,]+/).map((x)=>x.trim()).filter(Boolean))];}
function sourceLabel(item:Evidence){const p=item.provenance;if(/X Explore/i.test(p))return'X Trending';if(/X Home/i.test(p))return'X Home';if(/X Latest search/i.test(p))return'X Search';if(/TikTok Creative Center/i.test(p))return'TikTok Trending';if(/TikTok Explore/i.test(p))return'TikTok Explore';if(/TikTok search/i.test(p))return'TikTok Search';return item.platform;}

export default function BrowserIntelligence(){
 const [open,setOpen]=useState(false),[connected,setConnected]=useState(false),[busy,setBusy]=useState(''),[message,setMessage]=useState(''),[error,setError]=useState(''),[config,setConfig]=useState<BridgeConfig>(DEFAULT_CONFIG),[accounts,setAccounts]=useState(''),[keywords,setKeywords]=useState(''),[lastScan,setLastScan]=useState<ScanResult|null>(null),[narratives,setNarratives]=useState<Narrative[]>([]),[health,setHealth]=useState<BridgeHealth|null>(null);const flushing=useRef(false);
 const evidenceByPlatform=useMemo(()=>({x:lastScan?.evidence.filter((item)=>item.platform==='X').length||0,tiktok:lastScan?.evidence.filter((item)=>item.platform==='TikTok').length||0}),[lastScan]);
 const audit=useMemo(()=>{if(!lastScan)return null;const sourceCounts:Record<string,number>={};for(const item of lastScan.evidence){const key=sourceLabel(item);sourceCounts[key]=(sourceCounts[key]||0)+1;}const creators=new Set(lastScan.evidence.map((item)=>`${item.platform}:${item.author.toLowerCase()}`));const dated=lastScan.evidence.filter((item)=>item.published);const fresh24=dated.filter((item)=>(item.published||0)>=lastScan.at-86400000).length;const unknownAge=lastScan.evidence.length-dated.length;const topics=lastScan.inferredTopics?.length||0;let diagnosis='';if(!lastScan.evidence.length)diagnosis='No extractable X/TikTok evidence was collected. Check the source warnings and confirm the Front Chrome session is signed in.';else if(!topics)diagnosis=`Collection worked (${lastScan.evidence.length} items), but no topic reached strict Narrative Radar corroboration. Check Public Signals → Early Candidates instead of treating this as a failed scan.`;else diagnosis=`Collection and inference both worked: ${lastScan.evidence.length} evidence items produced ${topics} corroborated narrative${topics===1?'':'s'}.`;return{sourceCounts,uniqueCreators:creators.size,fresh24,unknownAge,topics,diagnosis};},[lastScan]);
 async function flushPending(silent=true){if(flushing.current)return;flushing.current=true;try{const pending=await local<{evidence:Evidence[];count:number;lastTopics?:InferredTopic[]}>('/pending');if(!pending.evidence.length)return;const stored=await saveEvidence(pending.evidence,pending.lastTopics||[]);await local('/ack',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({ids:pending.evidence.map((row)=>row.id)})});setNarratives((await fetchNarratives()).slice(0,8));window.dispatchEvent(new CustomEvent('front-browser-evidence-saved'));if(!silent)setMessage(`Synced ${stored.accepted||0} background evidence record(s), ${stored.inferredNarratives||0} inferred narrative(s), and ${stored.relationshipsSaved||0} relationship(s) into Front.`);}catch(e){if(!silent)setError((e as Error).message);}finally{flushing.current=false;}}
 async function ping(silent=false){try{const status=await local<BridgeHealth>('/health');setConnected(true);setHealth(status);setConfig(status.config||DEFAULT_CONFIG);setAccounts((status.config?.xAccounts||[]).join('\n'));setKeywords((status.config?.keywords||[]).join('\n'));if(!silent)setMessage(`Local browser intelligence connected · bridge v${status.version}${status.pendingCount?` · ${status.pendingCount} pending`:''}.`);void flushPending(true);return true;}catch{setConnected(false);if(!silent)setError('Local browser bridge is not running yet. Start browser-bridge/start.command on this Mac, then reconnect.');return false;}}
 useEffect(()=>{const kickoff=window.setTimeout(()=>{void ping(true);void flushPending(true);},0);const id=window.setInterval(()=>{void ping(true);void flushPending(true);},30000);return()=>{window.clearTimeout(kickoff);window.clearInterval(id);};},[]); // eslint-disable-line react-hooks/exhaustive-deps
 function toggleOpen(){const next=!open;setOpen(next);if(next)void ping(true);}
 async function saveConfig(){setBusy('config');setError('');setMessage('');try{const next={...config,xAccounts:lines(accounts),keywords:lines(keywords)};const result=await local<{config:BridgeConfig}>('/config',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(next)});setConfig(result.config);setMessage(`Scan settings saved. Automatic local scans run every ${result.config.intervalMinutes} minutes while the bridge and Front Chrome are running.`);}catch(e){setError((e as Error).message);}finally{setBusy('');}}
 async function openLogin(){setBusy('login');setError('');setMessage('');try{const result=await local<{message:string}>('/open-login',{method:'POST'},45_000);setConnected(true);setMessage(result.message);}catch(e){setError((e as Error).message);}finally{setBusy('');}}
 async function scan(){setBusy('scan');setError('');setMessage('Deep scan running. Front is sampling and scrolling X/TikTok feeds, then corroborating repeated topics. This can take several minutes.');try{const result=await local<ScanResult>('/scan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...config,xAccounts:lines(accounts),keywords:lines(keywords)})},DEEP_SCAN_TIMEOUT_MS);setLastScan(result);setMessage(`Collection finished: ${result.evidence.length} evidence record(s), ${result.inferredTopics?.length||0} corroborated topic(s). Saving evidence and candidates…`);const stored=await saveEvidence(result.evidence,result.inferredTopics||[]);setNarratives((await fetchNarratives()).slice(0,8));setConnected(true);window.dispatchEvent(new CustomEvent('front-browser-evidence-saved'));const warning=result.errors.length?` ${result.errors.length} source warning(s); details shown below.`:'';const promoted=stored.freshNarratives?` ${stored.freshNarratives} narrative${stored.freshNarratives===1?'':'s'} promoted.`:'';const relationships=stored.relationshipsSaved?` ${stored.relationshipsSaved} related context link${stored.relationshipsSaved===1?'':'s'} saved.`:'';const coins=stored.freshCoins?` ${stored.freshCoins} related coin candidate${stored.freshCoins===1?'':'s'} stored.`:'';const early=!stored.freshNarratives&&result.evidence.length?' No strict narrative was promoted yet; check Public Signals → Early Candidates.':'';setMessage(`Scan complete. Saved ${stored.accepted||0} evidence records. X ${result.evidence.filter((x)=>x.platform==='X').length} · TikTok ${result.evidence.filter((x)=>x.platform==='TikTok').length}.${promoted}${relationships}${coins}${early}${warning}`);void ping(true);}catch(e){setError((e as Error).message);}finally{setBusy('');}}
 return <><button onClick={toggleOpen} aria-label="Open browser intelligence" style={{position:'fixed',left:20,bottom:20,zIndex:70,border:0,borderRadius:999,padding:'12px 16px',fontWeight:800,background:'#fff',color:'#111',boxShadow:'0 10px 30px #0005',cursor:'pointer'}}><Activity size={16} style={{display:'inline',verticalAlign:'-3px',marginRight:7}}/>Browser Sources</button>{open&&<aside style={{position:'fixed',left:20,bottom:76,zIndex:69,width:'min(560px,calc(100vw - 28px))',maxHeight:'82vh',overflow:'auto',background:'#101216',color:'#f5f5f5',border:'1px solid #ffffff22',borderRadius:18,padding:18,boxShadow:'0 24px 60px #0009'}}><div style={{display:'flex',justifyContent:'space-between',gap:10,alignItems:'center'}}><div><strong>Social narrative discovery</strong><div style={{fontSize:12,opacity:.7}}>{connected?`● Bridge connected${health?.running?' · scanning':''}${health?.pendingCount?` · ${health.pendingCount} pending`:''}`:'○ Local bridge offline'}</div></div><button onClick={()=>setOpen(false)} aria-label="Close" style={{background:'none',border:0,color:'inherit'}}><X/></button></div><p style={{fontSize:13,opacity:.75}}>Trending pages seed discovery. Front samples X Home and TikTok Explore, keeps weaker repeats visible as Early Candidates, and only promotes stronger corroborated niche topics into Narrative Radar.</p><div style={{display:'flex',gap:8,flexWrap:'wrap'}}><button onClick={()=>void ping()} disabled={!!busy}>Reconnect</button><button onClick={()=>void openLogin()} disabled={!!busy}>{busy==='login'?'Opening…':'Open X + TikTok login'}</button><button onClick={()=>void scan()} disabled={!!busy||!connected}>{busy==='scan'?'Sampling + analyzing…':'Run deep scan'}</button><button onClick={()=>void flushPending(false)} disabled={!!busy||!connected}>Sync background finds</button></div><hr style={{borderColor:'#ffffff18'}}/><label style={{fontSize:12,fontWeight:700}}>High-signal X accounts</label><textarea value={accounts} onChange={(e)=>setAccounts(e.target.value)} rows={4} placeholder={'unusual_whales\nWatcherGuru\nother_account'} style={{width:'100%',boxSizing:'border-box',marginTop:6,padding:10,borderRadius:9,border:'1px solid #ffffff22',background:'#171a20',color:'inherit'}}/><label style={{fontSize:12,fontWeight:700,display:'block',marginTop:10}}>Narratives / keywords to hunt</label><textarea value={keywords} onChange={(e)=>setKeywords(e.target.value)} rows={3} placeholder={'Dejon Love\nviral meme phrase\ncelebrity moment'} style={{width:'100%',boxSizing:'border-box',marginTop:6,padding:10,borderRadius:9,border:'1px solid #ffffff22',background:'#171a20',color:'inherit'}}/><div style={{display:'flex',gap:8,alignItems:'center',marginTop:10,flexWrap:'wrap'}}><label style={{fontSize:12}}>Every <input type="number" min={10} max={240} value={config.intervalMinutes} onChange={(e)=>setConfig({...config,intervalMinutes:Number(e.target.value)})} style={{width:52}}/> min</label><label style={{fontSize:12}}>Scroll passes <input type="number" min={1} max={10} value={config.scrollPasses} onChange={(e)=>setConfig({...config,scrollPasses:Number(e.target.value)})} style={{width:45}}/></label><label style={{fontSize:12}}>Feed items <input type="number" min={10} max={120} value={config.maxFeedItems} onChange={(e)=>setConfig({...config,maxFeedItems:Number(e.target.value)})} style={{width:55}}/></label><label style={{fontSize:12}}>Expand topics <input type="number" min={0} max={10} value={config.inferredTopicSearches} onChange={(e)=>setConfig({...config,inferredTopicSearches:Number(e.target.value)})} style={{width:45}}/></label><button onClick={()=>void saveConfig()} disabled={!!busy}>{busy==='config'?'Saving…':'Save scan settings'}</button></div><div style={{display:'flex',gap:12,flexWrap:'wrap',marginTop:8,fontSize:12}}><label><input type="checkbox" checked={config.scanXHome} onChange={(e)=>setConfig({...config,scanXHome:e.target.checked})}/> X Home scroll</label><label><input type="checkbox" checked={config.scanXExplore} onChange={(e)=>setConfig({...config,scanXExplore:e.target.checked})}/> X trends</label><label><input type="checkbox" checked={config.scanTikTokExplore} onChange={(e)=>setConfig({...config,scanTikTokExplore:e.target.checked})}/> TikTok Explore scroll</label><label><input type="checkbox" checked={config.scanTikTokTrends} onChange={(e)=>setConfig({...config,scanTikTokTrends:e.target.checked})}/> TikTok trends</label></div><small style={{display:'block',opacity:.65,marginTop:8}}>Automatic collection stays local when this page is closed; up to 500 pending evidence records are queued on this Mac and synced when Front is open. The collector never bypasses CAPTCHAs, login challenges, rate limits, or platform blocks.</small>{health?.nextScheduledRun&&<small style={{display:'block',opacity:.65,marginTop:4}}>Next local scan: {new Date(health.nextScheduledRun).toLocaleTimeString()}</small>}{message&&<div style={{marginTop:12,padding:10,borderRadius:9,background:'#d5ff481a',fontSize:13}}>{message}</div>}{error&&<div style={{marginTop:12,padding:10,borderRadius:9,background:'#ff52521a',fontSize:13}}>{error}</div>}{lastScan&&audit&&<><hr style={{borderColor:'#ffffff18'}}/><strong>Scan audit</strong><div style={{display:'grid',gridTemplateColumns:'repeat(4,minmax(0,1fr))',gap:6,marginTop:8}}>{[['Evidence',lastScan.evidence.length],['Creators',audit.uniqueCreators],['Fresh <24h',audit.fresh24],['Radar',audit.topics]].map(([label,value])=><div key={String(label)} style={{padding:7,border:'1px solid #ffffff18',borderRadius:8}}><div style={{fontSize:10,opacity:.55}}>{label}</div><b>{value}</b></div>)}</div><div style={{fontSize:12,marginTop:8,padding:8,borderRadius:8,background:'#ffffff08'}}>{audit.diagnosis}</div><div style={{display:'flex',gap:6,flexWrap:'wrap',marginTop:7}}>{Object.entries(audit.sourceCounts).map(([label,count])=><span key={label} style={{fontSize:11,padding:'3px 6px',borderRadius:999,background:'#ffffff10'}}>{label} {count}</span>)}{audit.unknownAge>0&&<span style={{fontSize:11,padding:'3px 6px',borderRadius:999,background:'#ffffff10'}}>Age unknown {audit.unknownAge}</span>}</div>{lastScan.errors.map((value)=><div key={value} style={{fontSize:12,opacity:.72,marginTop:4}}>⚠ {value}</div>)}{(lastScan.inferredTopics||[]).slice(0,6).map((topic)=><div key={topic.key} style={{marginTop:8,padding:8,border:'1px solid #d5ff4833',borderRadius:9,fontSize:12}}><b>{topic.topic}</b> · {topic.authorCount} creators · {topic.evidenceCount} posts/videos · {topic.platforms.join(' + ')}<div style={{opacity:.65}}>Momentum evidence score {topic.score.toFixed(1)} · ranking signal, not a prediction</div>{(topic.relatedContexts||[]).length>0&&<div style={{marginTop:5,opacity:.82}}><b>Related context:</b> {(topic.relatedContexts||[]).slice(0,4).map((item)=>`${item.title} (${item.authorCount} creators)`).join(' · ')}</div>}</div>)}{lastScan.evidence.slice(0,8).map((item)=><div key={item.id} style={{marginTop:9,padding:9,border:'1px solid #ffffff18',borderRadius:9}}><div style={{fontSize:11,opacity:.65}}>{item.platform} · {item.author}{item.published?` · ${new Date(item.published).toLocaleString()}`:''}</div><div style={{fontSize:13,marginTop:3}}>{item.content.slice(0,220)}</div><a href={item.url} target="_blank" rel="noreferrer" style={{fontSize:12}}>Open evidence <ExternalLink size={11} style={{display:'inline'}}/></a></div>)}</>}{narratives.length>0&&<><hr style={{borderColor:'#ffffff18'}}/><strong>Detected narrative groups</strong>{narratives.map((item)=><div key={item.id} style={{marginTop:7,fontSize:13}}><b>{item.title}</b> · {item.stage} · {item.authors} account(s) · {item.platforms.join(' + ')}</div>)}</>}<div style={{marginTop:12,padding:10,borderRadius:9,background:'#ffffff0d',fontSize:12,display:'flex',gap:7}}><ShieldCheck size={15}/> X/TikTok cookies stay in ~/.front-browser-bridge/chrome-profile and are never uploaded to Front.</div></aside>}</>;
}
