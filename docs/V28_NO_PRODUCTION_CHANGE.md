# Front v28 Deployment State

This feature branch is pre-merge only. `main` and Railway production remain on the verified v27 release until repository-wide CI and real authenticated title-semantic acceptance pass and a merge is explicitly approved.
