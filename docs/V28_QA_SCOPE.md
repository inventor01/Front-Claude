# Front v28 QA Scope

Required before merge: focused title tests, full browser-bridge tests, security audit gates, migrations, typecheck, lint, production build, deploy-bundle validation, and one authenticated scan reviewed for title semantics. A failure in any required gate blocks merge.
